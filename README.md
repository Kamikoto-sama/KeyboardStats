# KeyboardStats

### Run

#### Manualy
Run `python main.py`

#### Windows background on log on
Add task to the Task Scheduler that runs `pythonw main.py` on user log on.
`pythonw` is needed in order to hide console window.

### Stats
Stat reports are saved every minute to the folder `reports` next to the `main.py`. One file per day.
